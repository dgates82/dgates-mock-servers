services:
  localstack:
    image: localstack/localstack:3
    container_name: dotnet-nuget-release-template-localstack
    ports:
      - "4566:4566"   # LocalStack edge port (all services)
    environment:
      - SERVICES=sns
      - DEBUG=0
    volumes:
      - "./localstack-data:/var/lib/localstack"
      - "/var/run/docker.sock:/var/run/docker.sock"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:4566/_localstack/health"]
      interval: 5s
      timeout: 5s
      retries: 10

  mailpit:
    image: axllent/mailpit:latest
    container_name: dgates-mock-servers-mailpit
    ports:
      - "1025:1025" # SMTP
      - "8025:8025" # web UI + REST API, used by MailpitClient in tests

  twiliomock:
    build:
      context: twilio-mock
    container_name: dgates-mock-servers-twiliomock
    ports:
      - "3030:3030" # web UI + Twilio-compatible REST API, used by TwilioMockClient in tests
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3030/health"]
      interval: 5s
      timeout: 5s
      retries: 10

  sendgridmock:
    build:
      context: sendgrid-mock
    container_name: dgates-mock-servers-sendgridmock
    ports:
      - "3040:3040" # web UI + SendGrid-compatible REST API, used by SendGridMockClient in tests
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3040/health"]
      interval: 5s
      timeout: 5s
      retries: 10

  postmarkmock:
    build:
      context: postmark-mock
    container_name: dgates-mock-servers-postmarkmock
    ports:
      - "3050:3050" # web UI + Postmark-compatible REST API, used by PostmarkMockClient in tests
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3050/health"]
      interval: 5s
      timeout: 5s
      retries: 10
